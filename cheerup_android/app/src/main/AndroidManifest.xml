<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.cheerup">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <service android:name=".MyFirebaseMessagingService">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
    </service>
    <service android:name=".MyFirebaseInstanceIDService">
        <intent-filter>
            <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
        </intent-filter>
    </service>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/MyMaterialTheme"
            android:label=""/>

        <activity android:name=".Activity.LoginActivity" />
        <activity
            android:name=".Activity.MyMailboxActivity"
            android:label="나의 응원함"
            android:theme="@style/MyMaterialTheme.PopupOverlay" />
        <activity
            android:name=".Activity.MyMailboxDetail_Activity"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name=".Activity.TutorialActivity" />
        <activity android:name=".Activity.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.WriteQActivity"
            android:label="응원의 말 보내기"
            android:theme="@style/MyMaterialTheme.PopupOverlay" />
        <activity android:name=".Activity.CardDetail_Activity"></activity>
    </application>

</manifest>